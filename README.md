# Funny-String This code determines if a string is "funny" by checking whether the absolute differences between adjacent characters are the same forwards and backwards. It reverses the string, compares these differences, and returns "Funny" if they match or "Not Funny" if they don’t.

